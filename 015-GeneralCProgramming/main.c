#include <stdio.h>
#include "utility.h"

int main()
{
    int x = -1;

    while (x) {
        bprint(x);
        x &= ~(1 << (rand() % 32));
        sleep(100);
    }

}


/****************************************************/
/****************************************************/







